'use strict'
/*
const express = require('express')
const router = express.Router()

const authMiddleware = require('../middleware/auth');
const roleManagerMiddleware = require('../middleware/has_role_manager');
const mDataController = require('../controllers/meteorologic_data');

router.post('/weather', authMiddleware, roleManagerMiddleware, mDataController.nuevaData);
router.get('/weather/:id', authMiddleware, mDataController.getDataById);
router.get('/stations/:id/weather/', authMiddleware, mDataController.getDataByStationId);
router.get('/stations/:id/weather/from/:from/to/:to', authMiddleware, mDataController.getDataByStationId);

module.exports = router
*/